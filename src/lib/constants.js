// Résumé lives in /public. Drop the actual PDF at this path to enable the
// download buttons across the site. Kept in a plain (non-"use client") module
// so both server and client components can import the value directly.
export const RESUME_URL = "/Resume_Lucio_Ruben_Villena.pdf";
